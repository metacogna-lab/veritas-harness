# Harness State — 2026-07-09T17:16

Session snapshot comparing completed work against `agents/plans/`. Canonical harness path:
**`harness/veritas-research/`**. Branch policy: one branch per work unit; add, commit, push on
completion.

---

## Session metadata

| Field | Value |
|-------|-------|
| Date | 2026-07-09 |
| Base branch | `harness-build` |
| Canonical harness | `harness/veritas-research/` |
| Workflow | New branch per unit → implement → `bun test` (+ scripts) → commit → push |
| Plans | `agents/plans/00-overview.md` through `06-risk-register.md` |

---

## Completed work

### Phase 01 BASIC — DONE

All modules built in dependency order (parse → config → llm → scope → mission → tools → agent).

| Task | Module | Status |
|------|--------|--------|
| 1.0 Project init | `package.json`, `tsconfig.json` | Done |
| 1.1 Provider abstraction | `src/llm/` | Done |
| 1.2 Config + redact | `src/config/` | Done |
| 1.4 Scope gate | `src/safety/scope.ts` | Done |
| 1.5 Tool registry | `src/tools/` | Done |
| 1.6 JSON parse | `src/parse/json.ts` | Done |
| 1.7 Mission object | `src/mission/` | Done |
| 1.3 ReAct loop | `src/agent/index.ts` | Done |

**Verification:** `bun test` green (132+ pass). Starter tools tiered `active` (not literal `safe`);
scope gate applies to reads too. See `agents/state/build-log.md` for judgment calls.

### Phase 02 INT — DONE (artifacts split across trees pre-consolidation)

| Task | Module | Status |
|------|--------|--------|
| 2.1 Loadouts | `src/agent/specialists.ts`, `loadouts.ts` | Done |
| 2.2 Approval gate | `src/safety/approval.ts`, `index.ts` | Done |
| 2.3 Evidence gate | `src/evidence/gate.ts` | Done |
| 2.4 Refuter | `src/evidence/refuter.ts` | Done |
| 2.5 Control plane | `src/control/`, `src/cli.ts` | Done |
| 2.6 verify-claims | `scripts/verify-claims.mjs` | In `general-purpose` only (Unit 1) |
| 2.7 doctor | `scripts/doctor.mjs` | In `general-purpose` only (Unit 1) |

**Deferred:** HTTP server (`src/server.ts`) — CLI covers DoD.

### Phase 03 ADV — PARTIAL

| Task | Status | Notes |
|------|--------|-------|
| 3.1 Orchestrator | Partial | In `general-purpose/src/orchestration/`; Unit 1 ports to canonical |
| 3.2 Benchmark harness | Partial | `bench.mjs`, `guard.ts`, `scope-gate` suite in veritas-research; missing `stats.mjs` |
| 3.3 Human-gated terminal actions | Not started | Unit 3 |
| 3.4 Lessons loop | Not started | Unit 4 |

### Phase 04 Skills + Consumability — NOT STARTED

Units 5–6.

---

## Consolidation note (Unit 1)

Work was split across two trees. Port from `harness/general-purpose/` → `harness/veritas-research/`:

- `scripts/doctor.mjs`, `verify-claims.mjs`, `verify-finding.mjs`, `gen-int-smoke.mjs`, `lib/stats.mjs`
- `bench/int-smoke/`, `claims.json`
- `.githooks/pre-push`
- `src/orchestration/`

Then delete `harness/general-purpose/`.

---

## Remaining work units

### Unit 0 — `state/consolidation-plan` ← this file

- **DoD:** State file committed and pushed.

### Unit 1 — `feat/harness-consolidate`

- **Plan refs:** 02 §2.6–2.7, 03 §3.1
- **Files:** merge scripts, orchestration, int-smoke into `harness/veritas-research/`
- **DoD:** `bun test && bun run doctor && bun run verify-claims` exit 0; `general-purpose/` removed

### Unit 2 — `feat/adv-bench`

- **Plan refs:** 03 §3.2, 05 benchmark rules
- **Files:** `scripts/bench.mjs`, `bench/scope-gate/results.json`, `claims.json`
- **DoD:** `bun run bench` green; `verify-claims` re-derives bench pass@1 (black-box + white-box separate)

### Unit 3 — `feat/adv-human-gate`

- **Plan refs:** 03 §3.3, invariant #5
- **Files:** `src/safety/human-release.ts` (or similar), agent/control-plane wiring, e2e test
- **DoD:** unattended run never auto-executes terminal action; `bun test` green

### Unit 4 — `feat/adv-lessons`

- **Plan refs:** 03 §3.4
- **Files:** `src/resources/lessons.ts`, `scripts/lessons.mjs`
- **DoD:** one mission produces committed lesson; `retrieveLessons()` returns it

### Unit 5 — `feat/skills`

- **Plan refs:** 04 Part A
- **Files:** `skills/harness-tool-adder/SKILL.md`, `harness-eval-runner/SKILL.md`, `harness-refuter/SKILL.md`
- **DoD:** three skills exist; one exercise each in `agents/evals/`

### Unit 6 — `feat/consumability`

- **Plan refs:** 04 Part B
- **Files:** `.claude/commands/`, `.claude/agents/refuter.md`, `src/mcp-server.ts`
- **DoD:** MCP no-bypass test passes; CLAUDE.md synced

### Unit 7 — `chore/initial-harness-commit`

- **Plan refs:** 00 overview DoD, 05 cross-cutting
- **Files:** full `harness/veritas-research/`, `agents/`, `CLAUDE.md`
- **DoD:** clean `git status`; all verification scripts green

---

## Verification gates (every unit boundary)

```bash
cd harness/veritas-research
bun test
bun run doctor
bun run verify-claims
# after Unit 2+
bun run bench
```

Record eval notes in `agents/evals/<unit-id>.md`.

---

## Open questions (from plan 06)

- [ ] Which providers are available; what should `defaultProvider` be for the first real Loadout?
- [ ] What is the first real objective/Loadout for smoke test and first benchmark suite?
- [ ] Does `~/.harness/config.json` differ per project sub-folder?
- [ ] Does the HTTP API need auth before exposure beyond localhost?

---

## Risk items still open

- Pre-push hook file exists but is **not** auto-enabled (`core.hooksPath` affects whole repo)
- HTTP server deferred (plan 02 §2.5 optional)
- `agents/evals/` empty until Units 5–7
