# Harness API base URL. In dev, leave this unset — vite.config.ts proxies /v1 and
# /health to HARNESS_API_URL (default http://localhost:8080), so no CORS needed.
# Set this only when serving the built SPA against a remote API.
# VITE_HARNESS_API_URL=https://harness.example.com
