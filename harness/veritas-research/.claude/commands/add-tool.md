Add a new tool named `$ARGUMENTS` to the harness ToolRegistry using the harness-tool-adder skill at `harness/veritas-research/skills/harness-tool-adder/SKILL.md`.

Pick the correct riskTier, wire the safety gate, register it in `src/tools/index.ts`, and add tests. Report the riskTier you chose and why before finishing.

Do not mark done until `bun test` is green in `harness/veritas-research/`.
