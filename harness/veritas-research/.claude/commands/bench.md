Run the harness-eval-runner skill against benchmark suite `$ARGUMENTS`.

Read `harness/veritas-research/scripts/bench.mjs`, run `bun run bench $ARGUMENTS`, then `bun run verify-claims`. Do not report numbers if verify-claims fails.

Skill: `harness/veritas-research/skills/harness-eval-runner/SKILL.md`
